{
  "name": "@tionkje/queueproto",
  "version": "0.0.1",
  "scripts": {
    "dev": "svelte-kit dev",
    "build": "svelte-kit build",
    "package": "svelte-kit package",
    "preview": "svelte-kit preview",
    "lint": "prettier --check --plugin-search-dir=. .",
    "format": "prettier --write --plugin-search-dir=. .",
    "prepare": "husky install"
  },
  "devDependencies": {
    "@sveltejs/adapter-auto": "next",
    "@sveltejs/kit": "next",
    "husky": "^7.0.4",
    "prettier": "^2.5.1",
    "prettier-plugin-svelte": "^2.5.1",
    "svelte": "^3.44.3"
  },
  "type": "module",
  "dependencies": {
    "@tionkje/queuelib": "latest"
  },
  "lint-staged": {
    "*.{js,css,md,svelte}": "prettier --write --plugin-search-dir=. ."
  }
}
